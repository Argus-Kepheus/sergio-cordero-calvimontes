# Guidance for AI agents

## Branch policy

- Work directly on `main`: commit and push every change to `main`.
- Do not create branches or pull requests unless the repository owner
  explicitly asks for one in the current request.
- If the environment forces a separate working branch, merge it into `main`
  (fast-forward when possible), push `main`, and delete the temporary branch
  once its content is on `main`.
